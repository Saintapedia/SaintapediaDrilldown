{
	"name": "mediawiki-extension-saintapediadrilldown",
	"description": "MediaWiki extension: sidebar layout and filter chips for Cargo Special:Drilldown",
	"version": "0.6.3",
	"private": true,
	"scripts": {
		"test": "grunt test && npm run lint:css && node --test",
		"lint:css": "stylelint \"modules/**/*.css\"",
		"unit": "node --test"
	},
	"devDependencies": {
		"eslint-config-wikimedia": "^0.32.5",
		"grunt": "^1.6.3",
		"grunt-banana-checker": "^0.13.0",
		"grunt-eslint": "^24.0.0",
		"grunt-jsonlint": "^3.0.0",
		"stylelint": "^17.15.0",
		"stylelint-config-wikimedia": "^0.19.3"
	}
}
